{
  "name": "dbaronx-telegram-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python -m compileall src",
    "typecheck": "python -m compileall src",
    "dev": "python -m src.main",
    "start": "python -m src.main",
    "deps:install": "python -m pip install -r requirements.txt",
    "smoke:runtime": "TELEGRAM_BOT_TOKEN=dummy NESTJS_BASE_URL=http://localhost:3001 FASTAPI_BASE_URL=http://localhost:8080 INTERNAL_SERVICE_TOKEN=dummy PYTHONPATH=src python -c \"import main; print('import-ok')\""
  }
}
