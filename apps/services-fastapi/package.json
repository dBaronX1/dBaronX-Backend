{
  "name": "dbaronx-fastapi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python -m compileall src",
    "typecheck": "python -m compileall src",
    "dev": "PYTHONPATH=src python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8080",
    "start": "PYTHONPATH=src python -m uvicorn src.main:app --host 0.0.0.0 --port 8080"
  }
}
