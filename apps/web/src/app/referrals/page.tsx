import { RocketSimplePage } from "@/components/rocket/StaticPages";
export default function ReferralsPage() { return <RocketSimplePage title="Referrals" description="Invite tracking keeps referral parameters flowing through registration, login, and auth callback routes." />; }
