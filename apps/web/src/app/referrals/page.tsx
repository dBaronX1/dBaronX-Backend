import { DbxSimplePage } from "@/components/dbx/StaticPages";
export default function ReferralsPage() { return <DbxSimplePage title="Referrals" description="Invite tracking keeps referral parameters flowing through registration, login, and auth callback routes." />; }
